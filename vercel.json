{
  "build": {
    "outputDirectory": "dist",
    "command": "npm run build",
    "installCommand": "npm install"
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}